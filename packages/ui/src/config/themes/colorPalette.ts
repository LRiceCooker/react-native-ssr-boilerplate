import tokens from '../tokens'

const light = {
    primary: tokens.color.lprimary,
    success: tokens.color.lsuccess,
    warning: tokens.color.lwarning,
    secondary: tokens.color.lsecondary,
    light: tokens.color.llight,
    muted: tokens.color.lmuted,
    danger: tokens.color.ldanger,
    dark: tokens.color.ldark,
    info: tokens.color.linfo,
    black: tokens.color.lblack,
    white: tokens.color.lwhite,
}

const dark = {
    primary: tokens.color.dprimary,
    success: tokens.color.dsuccess,
    warning: tokens.color.dwarning,
    secondary: tokens.color.dsecondary,
    light: tokens.color.dlight,
    muted: tokens.color.dmuted,
    danger: tokens.color.ddanger,
    dark: tokens.color.ddark,
    info: tokens.color.dinfo,
}

export {light, dark};